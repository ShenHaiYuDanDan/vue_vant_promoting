配置说明：
本地测试不能在axios中直接写死地址，如果写死，这个网页加载的就是这个vue死地址，比如:localohst:8080,那么他请求的就是当前用户手机上的8080这个端口。而用户手机上是没有8080服务的。

而在vue.config.js配置是配置的服务器地址，不同于直接写在请求上，在初始加载的时候这个localhost:8081这个地址随着请求而变化，相当于如果是本地请求 是localhost:8081，如果按照ip请求

那么他的ip地址变成本机真实的ip地址，类似于192.168.1.117:8080这种格式

配置文件中localhost:8081代替了当前服务发布地址，访问也自然是发放服务的服务器

而写在vue文件中，用户加载页面的时候是也带出来了这个localhost，这个时候他是本机地址，不是配置文件，而vue.config是配置文件，代理了发布服务的地址，如192.168.1.117。